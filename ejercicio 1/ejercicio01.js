'use strict'
//Escribir un programa que extraiga las parte numérica de una placa de un auto.

let plate = 'ABC-2030';
let numeros = plate.substring(plate.indexOf('-') + 1);
alert(numeros);














